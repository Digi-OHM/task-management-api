<configuration>
    
    <!-- ✅ ปิด warn จาก Spring Data PageImpl Serialization -->
    <logger name="org.springframework.data.web.config.SpringDataJackson3Configuration$PageModule$WarningLoggingModifier" level="OFF"/>


    <property name="CONSOLE_LOG_PATTERN"
              value="%yellow([%d{yyyy-MM-dd' 'HH:mm:ss}]) %highlight(%-5level) %cyan(%logger{36}) : %msg%n"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>
